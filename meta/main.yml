galaxy_info:
  author: bonovoxly
  description: "Gets the public SSH key from various cloud providers (Google Cloud, AWS, and Azure) and adds them to the known_hosts file. Also configures ~/.ssh/config for human readability (i.e., ssh user@server)."
  company: Little Fluffy Clouds
  license: BSD
  min_ansible_version: 2.5
  platforms:
    - name: MacOSX
      versions:
        - all
    - name: EL
      versions:
        - all
    - name: Fedora
      versions:
        - all
    - name: Debian
      versions:
        - all
    - name: Ubuntu
      versions:
        - all
  galaxy_tags: []
    - gcp
    - gcloud
    - google
    - ssh

